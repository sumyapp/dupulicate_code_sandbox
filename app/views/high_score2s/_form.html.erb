<%= form_with(model: high_score2, local: true) do |form| %>
  <% if high_score2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(high_score2.errors.count, "error") %> prohibited this high_score2 from being saved:</h2>

      <ul>
        <% high_score2.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game %>
    <%= form.text_field :game %>
  </div>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
